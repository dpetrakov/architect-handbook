# OpenAPI спецификация — [Название API]
# Замените содержимое на актуальное для вашего проекта

openapi: 3.1.0
info:
  title: "[Название] API"
  version: 0.1.0
  description: TODO

servers:
  - url: https://api.example.com/v1
    description: Production

paths:
  /health:
    get:
      summary: Health check
      operationId: healthCheck
      responses:
        '200':
          description: OK

  # TODO: добавьте ваши endpoints

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
